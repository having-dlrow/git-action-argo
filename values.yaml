nameOverride: ""
fullnameOverride: ""

replicaCount: 1
application:
  port: 80
  profile: dev

service:
  type: ClusterIP
  port: 80
  targetPort: 80
  nodePort:

serviceAccount:
  create: false
  annotations: {}
  name: ""

image:
  repository: harbor.ha-ving.store/docker/sample-springboot
  tag: "3a1b34dc75c68bf190e57d377dae72d97f7fb894"
  pullPolicy: IfNotPresent

podAnnotations: {}
podSecurityContext: {}
securityContext: {}

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 60
  targetMemoryUtilizationPercentage: 60

ingress:
  enabled: true
  ingressClassName: "nginx"  
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "ha-ving.store-issuer"
  hosts:
    - host: springboot.ha-ving.store
      path:
        - path: /
          pathType: Prefix
  tls:
    - secretName: springboot-ingress
      hosts:
        - springboot.ha-ving.store

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 250m
    memory: 256Mi

volumes: []
# - name: foo
#   secret:
#     secretName: mysecret
#     optional: false

nodeSelector: {}

tolerations: []

affinity: {}